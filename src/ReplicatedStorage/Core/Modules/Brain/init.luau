--!strict
-- Brain: reusable state-machine and behavior-tree primitives.

return {
	StateMachine = require(script.StateMachine),
	BehaviorTree = require(script.BehaviorTree),
	Nodes = require(script.Nodes),
}
