--!strict
-- Shared custom enum definitions for framework systems.

return table.freeze({
	Brain = require(script.Brain),
	Motion = require(script.Motion),
	NPC = require(script.NPC),
	Collectibles = require(script.Collectibles),
	Common = require(script.Common),
})
